name: CD with Docker

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout código
      uses: actions/checkout@v3

    - name: Login no Docker Hub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    - name: Build da imagem Docker
      run: docker build -t tassoedcc/secretarIA:${{ github.sha }} .

    - name: Push da imagem para Docker Hub
      run: docker push tassoedcc/secretarIA:${{ github.sha }}

    - name: Tag 'latest'
      run: |
        docker tag tassoedcc/secretarIA:${{ github.sha }} tassoedcc/secretarIA:latest
        docker push tassoedcc/secretarIA:latest
